# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
  test8-3:
    docker:
      - image: cimg/php:8.3
    steps:
      - checkout
      # Download and cache dependencies
      - restore_cache:
          keys:
            - 8.3-dependencies-{{ checksum "composer.json" }}
            - 8.3-dependencies-
      - run:
          name: "Install Dependencies"
          command: composer install --no-interaction --prefer-source
      - save_cache:
          key: 8.3-dependencies-{{ checksum "composer.json" }}
          paths:
            - ./vendor
      - run:
          name: "Run Tests"
          command: vendor/bin/phpunit --testdox

  test8-4:
    docker:
      - image: cimg/php:8.4
    steps:
      - checkout
      # Download and cache dependencies
      - restore_cache:
          keys:
            - 8.4-dependencies-{{ checksum "composer.json" }}
            - 8.4-dependencies-
      - run:
          name: "Install Dependencies"
          command: composer install --no-interaction --prefer-source
      - save_cache:
          key: 8.4-dependencies-{{ checksum "composer.json" }}
          paths:
            - ./vendor
      - run:
          name: "Run Tests"
          command: vendor/bin/phpunit --testdox

# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  tests_versions:
    jobs:
      - test8-3
      - test8-4
